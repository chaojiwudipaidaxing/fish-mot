# Tested on the local paper-release environment with Python 3.11.9.
# If the exact torch wheel is not available for your platform, install a
# compatible PyTorch build first and then install the remaining packages.

numpy==2.3.5
scipy==1.16.3
matplotlib==3.10.7
Pillow==12.0.0
pandas==2.3.3
PyYAML==6.0.3
tqdm==4.67.1
torch==2.9.1
psutil==7.1.3

# Optional, not required for the core MOTChallenge2DBox runs used in the paper:
# pycocotools
